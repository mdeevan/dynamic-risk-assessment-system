name: diagnostics
conda_env: ../../config/environment.yml

entry_points:
  main:
    parameters:

      model_path:
        description: ## ADD DESCRIPTION
        type: string

       model_name:
        description: ## ADD DESCRIPTION
        type: string


    command: >-
        python diagnostics.py  --model_path {model_path}  -- model_name { model_name} 
